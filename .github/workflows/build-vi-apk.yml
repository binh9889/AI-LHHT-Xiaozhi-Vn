name: Build AI-LHHT v4 Realtime Tools Pro APK

on:
  workflow_dispatch:
  push:
    branches: [ main, develop-v3, develop-v4 ]
    paths:
      - 'lib/**'
      - 'android/**'
      - 'ios/**'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - 'test/**'
      - 'scripts/**'
      - '.github/workflows/build-vi-apk.yml'

jobs:
  build-android:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.2'
          channel: 'stable'
          cache: true

      - name: Flutter version
        run: flutter --version

      - name: Get dependencies
        run: flutter pub get

      - name: Static v4 source checks
        run: bash scripts/verify_v4.sh --static-only

      - name: Analyze Dart
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: UI and unit tests
        run: >-
          flutter test
          test/widget_test.dart
          test/minimax_test.dart
          test/pcm_frame_buffer_test.dart
          test/xiaozhi_websocket_manager_test.dart
          test/interpreter_turn_controller_test.dart
          test/realtime_tool_service_test.dart
          test/tool_registry_test.dart
          test/tool_intent_router_test.dart
          test/realtime_tool_engine_local_test.dart

      - name: Fix flutter_pcm_player Android namespace
        shell: bash
        run: |
          PCM_DIR="$HOME/.pub-cache/hosted/pub.dev/flutter_pcm_player-0.0.1/android"
          GRADLE="$PCM_DIR/build.gradle"
          MANIFEST="$PCM_DIR/src/main/AndroidManifest.xml"
          if [ -f "$GRADLE" ]; then
            if grep -q 'namespace' "$GRADLE"; then
              sed -i 's/namespace "com.example.flutter_pcm_player"/namespace "com.lhht.flutter_pcm_player"/' "$GRADLE"
            else
              sed -i '/android {/a\    namespace "com.lhht.flutter_pcm_player"' "$GRADLE"
            fi
          fi
          if [ -f "$MANIFEST" ]; then
            sed -i 's/package="com.example.flutter_pcm_player"/package="com.lhht.flutter_pcm_player"/' "$MANIFEST"
          fi

      - name: Build APK
        run: flutter build apk --debug --build-name=4.0.0 --build-number=400

      - name: Rename APK
        run: cp build/app/outputs/flutter-apk/app-debug.apk AI-LHHT-v4.0.0-Realtime-Tools-Pro-VI.apk

      - name: SHA256
        run: sha256sum AI-LHHT-v4.0.0-Realtime-Tools-Pro-VI.apk | tee AI-LHHT-v4.0.0-Realtime-Tools-Pro-VI.apk.sha256

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: AI-LHHT-v4.0.0-Realtime-Tools-Pro-VI
          retention-days: 30
          path: |
            AI-LHHT-v4.0.0-Realtime-Tools-Pro-VI.apk
            AI-LHHT-v4.0.0-Realtime-Tools-Pro-VI.apk.sha256
